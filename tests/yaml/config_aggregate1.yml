input:
  grid: "tests/data/reek_3d_maps/REEK.EGRID"
  properties:
    - source: "tests/data/reek_3d_maps/REEK.UNRST"
      name: "SWAT"
      lower_threshold: 1e-12

output:
  mapfolder: tmp
  replace_masked_with_zero: no
